{
  "name": "babymo-bedtime",
  "version": "1.0.0",
  "description": "Baby Mo Bedtime Ritual App — Islamic kids bedtime companion",
  "author": "Baby Mo Team <hello@babymo.id>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "android": "npx cap run android",
    "ios": "npx cap run ios",
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "build": "echo 'No build step needed — pure HTML app' && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "@capacitor/app": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
